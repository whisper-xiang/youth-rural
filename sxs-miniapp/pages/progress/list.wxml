<view class="page">
  <!-- 顶部操作栏 -->
  <view class="header">
    <view class="header-title">项目进度</view>
  </view>

  <!-- 项目列表 -->
  <view class="project-list" wx:if="{{projects.length > 0}}">
    <view class="project-item" wx:for="{{projects}}" wx:key="id" bindtap="goProject" data-id="{{item.id}}" data-title="{{item.title}}">
      <view class="project-header">
        <view class="project-title">{{item.title}}</view>
        <view class="project-status status-{{item.status}}">{{item.statusText}}</view>
      </view>
      <view class="project-info">
        <text class="info-item">📍 {{item.target_area}}</text>
        <text class="info-item">📅 {{item.start_date}} - {{item.end_date}}</text>
      </view>
      <view class="project-progress">
        <view class="progress-count">
          <text class="count-number">{{item.progress_count || 0}}</text>
          <text class="count-label">个进度</text>
        </view>
        <view class="progress-arrow">→</view>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty" wx:else>
    <view class="empty-icon">📋</view>
    <view class="empty-text">暂无相关项目</view>
    <view class="empty-hint">请先申报项目或联系管理员</view>
  </view>
</view>
